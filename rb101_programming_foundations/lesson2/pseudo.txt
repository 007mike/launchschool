START

DEFINE a method that takes two integer parameters
SET answer as the SUM of the first integer and the second integer
RETURN answer 

END

START

DEFINE a method that takes an array of strings as a parameter
LOOP through each element of the array
SET a new array inside the loop equal to itself plus the current element from the array of strings being iterated
RETURN the new array

END

START

DEFINE a method that takes an array of integers as a parameter
LOOP through each element in the array
SET a new array inside the loop equal to itself plus the current element from the array of integers being iterated but only if that element is odd

RETURN the new array